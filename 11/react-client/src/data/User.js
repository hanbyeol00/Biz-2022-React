export class User {
  constructor(username, password, re_password, nickname, birthdate, level) {
    this.username = username;
    this.password = password;
    this.re_password = re_password;
    this.nickname = nickname;
    this.birthdate = birthdate;
    this.level = level;
  }
}
